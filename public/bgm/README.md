# Shadow Garden BGM Armory
Upload your `.mp3`, `.wav`, or `.ogg` audio files to this folder (`public/bgm/`).
The system automatically discovers and randomly plays background music tracks from this folder when entering the portal sequence.
